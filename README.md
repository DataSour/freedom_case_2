# 🔥 FIRE — Freedom Intelligent Routing Engine

Сервис автоматической обработки и распределения обращений клиентов, поступивших в нерабочие часы. Система анализирует входящие тикеты с помощью ИИ (LLM), обогащает данные (классификация, тональность, гео-кодинг) и назначает каждое обращение на наиболее подходящего менеджера с соблюдением бизнес-правил и балансировки нагрузки.

---

## Архитектура

```
CSV (tickets, managers, business_units)
        │
        ▼
┌──────────────────────┐
│   AI-анализ (Groq)   │  ← llama-3.3-70b (текст) / llama-4-scout (изображения)
│  тип, тональность,   │
│  приоритет, язык,     │
│  summary              │
└──────────┬───────────┘
           ▼
┌──────────────────────┐
│   Геокодинг клиента  │  ← Nominatim (OpenStreetMap)
│   (адрес → lat/lon)  │
└──────────┬───────────┘
           ▼
┌──────────────────────┐
│  Ближайший офис      │  ← Haversine formula
│  (15 офисов по КЗ)   │
└──────────┬───────────┘
           ▼
┌──────────────────────┐
│  Назначение менеджера │  ← Каскад фильтров + Round Robin
└──────────┬───────────┘
           ▼
      final_results.csv
```

---

## Функциональность

### 1. AI-анализ текста (NLP)

Каждое обращение анализируется через **Groq API** (модели LLaMA) и возвращает:

| Поле | Описание |
|------|----------|
| `type` | Тип обращения: Жалоба, Смена данных, Консультация, Претензия, Неработоспособность приложения, Мошеннические действия, Спам |
| `sentiment` | Тональность: Позитивный, Нейтральный, Негативный |
| `priority` | Приоритетность: 1–10 |
| `language` | Язык: KZ, ENG, RU (по умолчанию RU) |
| `summary` | Краткая суть + рекомендация для менеджера |

- **Текстовые обращения** → `llama-3.3-70b-versatile` с `response_format: json_object`
- **Изображения (вложения)** → `llama-4-scout-17b-16e-instruct` (vision) с regex-фолбэком для извлечения JSON
- **Спам** — классифицируется, но менеджер не назначается

### 2. Гео-нормализация

- Адрес клиента преобразуется в координаты (lat/lon) через **Nominatim**
- Координаты офисов кешируются в `units_coords_cache.json` для ускорения повторных запусков
- Составные названия городов (например, `Косшы / Астана`) автоматически очищаются

### 3. Логика распределения (Business Rules)

Каскад фильтров:

1. **Географический фильтр** — поиск ближайшего офиса по формуле Гаверсинуса
   - Клиенты с неизвестным адресом или из-за рубежа → чередование 50/50 между Астаной и Алматы

2. **Фильтр компетенций (Hard Skills)**:
   - **VIP / Priority сегмент** → только менеджеры с навыком `VIP`
   - **Смена данных** → только менеджеры с должностью `Главный специалист`
   - **Язык KZ / ENG** → менеджер должен иметь соответствующий навык

3. **Балансировка (Round Robin)**:
   - Выбираются 2 менеджера с наименьшей нагрузкой
   - Обращения распределяются между ними поочерёдно

4. **Каскадный поиск по офисам** — если в ближайшем офисе нет подходящих менеджеров, система ищет в следующем по удалённости

---

## Входные данные

Три CSV-файла:

### `tickets.csv` — Обращения клиентов
| Колонка | Описание |
|---------|----------|
| GUID клиента | Уникальный идентификатор |
| Пол клиента | Мужской / Женский |
| Дата рождения | Дата рождения клиента |
| Описание | Текст обращения (свободный ввод) |
| Вложения | Имя файла изображения (если есть) |
| Сегмент клиента | Mass, VIP, Priority |
| Страна, Область, Населённый пункт, Улица, Дом | Адрес клиента |

### `managers.csv` — Менеджеры
| Колонка | Описание |
|---------|----------|
| ФИО | Имя менеджера |
| Должность | Специалист, Ведущий специалист, Главный специалист |
| Офис | Бизнес-единица (город) |
| Навыки | Массив: VIP, ENG, KZ |
| Количество обращений в работе | Текущая нагрузка |

### `business_units.csv` — Офисы
| Колонка | Описание |
|---------|----------|
| Офис | Название города |
| Адрес | Физический адрес офиса |

---

## Выходные данные

Файл `final_results.csv` содержит:

| Колонка | Описание |
|---------|----------|
| GUID клиента | Идентификатор клиента |
| Сегмент клиента | Mass / VIP / Priority |
| Описание | Текст обращения (до 120 символов) |
| ai_type | Тип обращения (AI) |
| ai_sentiment | Тональность (AI) |
| ai_priority | Приоритет 1–10 (AI) |
| ai_language | Язык обращения (AI) |
| ai_summary | Краткая суть + рекомендация (AI) |
| client_lat / client_lon | Координаты клиента |
| nearest_office | Ближайший офис |
| assigned_manager | Назначенный менеджер |

---

## Установка и запуск

### Требования

- Python 3.10+
- API-ключ [Groq](https://console.groq.com/)

### Установка

```bash
git clone https://github.com/<your-repo>/freedom-fire.git
cd freedom-fire
pip install -r requirements.txt
```

### Настройка

Создайте файл `.env` в корне проекта:

```env
GROQ_API_KEY=gsk_ваш_ключ_здесь
```

### Запуск

```bash
python main.py
```

Доступно два режима:

- **Режим 1 — Интерактивный**: ввод обращений вручную, мгновенный анализ и назначение
- **Режим 2 — Обработка CSV**: полный pipeline по всем тикетам из `tickets.csv`

---

## Стек технологий

| Компонент | Технология |
|-----------|-----------|
| Язык | Python 3.13 |
| LLM | Groq API (LLaMA 3.3 70B + LLaMA 4 Scout Vision) |
| Геокодинг | Geopy + Nominatim (OpenStreetMap) |
| Данные | Pandas |
| Конфигурация | python-dotenv |

---

## Структура проекта

```
freedom_case2/
├── main.py                  # Основной код (pipeline, AI, геокодинг, распределение)
├── tickets.csv              # Входные данные: обращения клиентов
├── managers.csv             # Входные данные: менеджеры
├── business_units.csv       # Входные данные: офисы
├── requirements.txt         # Зависимости Python
├── .env                     # API-ключ Groq (не коммитить!)
├── units_coords_cache.json  # Кеш координат офисов (генерируется автоматически)
├── final_results.csv        # Результат обработки (генерируется автоматически)
├── order_error.png          # Вложение к тикету (изображение)
├── data_error.png           # Вложение к тикету (изображение)
└── README.md
```

---

## Особенности реализации

- **Кеширование геокодинга офисов** — координаты сохраняются в JSON, повторные запуски не требуют обращения к Nominatim
- **Retry-логика для AI** — до 3 попыток при невалидном JSON, с regex-фолбэком для vision-модели
- **Каскадный поиск менеджера** — если в ближайшем офисе нет подходящего, ищет в следующих по удалённости
- **Обработка вложений** — изображения анализируются vision-моделью наравне с текстом
- **Fallback для адресов** — неизвестные/зарубежные клиенты распределяются 50/50 между Астаной и Алматы
- **Round Robin** — честная поочерёдная балансировка между двумя наименее загруженными менеджерами
